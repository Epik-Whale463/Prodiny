# Render Blueprint for Prodiny Platform
# This file defines infrastructure as code for the entire application
# Updated: Force redeploy with correct backend URL prodiny.onrender.com

services:
  # Backend API Service
  - type: web
    name: prodiny-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: prodiny-db
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://prodiny-frontend.onrender.com,http://localhost:3000
    healthCheckPath: /health
    
  # Frontend Web Service
  - type: web
    name: prodiny-frontend
    runtime: node
    buildCommand: cd frontend && npm ci && npm run build
    startCommand: cd frontend && npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://prodiny.onrender.com
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000

databases:
  # PostgreSQL Database
  - name: prodiny-db
    databaseName: prodiny_production
    user: prodiny_user
