# Prodiny Firebase Backend

## Environment Variables
NODE_ENV=production
PORT=3001
CORS_ORIGINS=https://prodiny-frontend.onrender.com,http://localhost:3000

# Firebase configuration is in firebase-config.js
# No additional environment variables needed for Firebase client SDK
